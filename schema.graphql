type EthosModule {
  id: ID!
  name: String!
  address: String!
  categoryName: String!
  categoryId: BigInt!
}

type Mission {
  id: ID!
  title: String!
  description: String!
  address: String!
  sponsor: String!
  startDate: BigInt!
  endDate: BigInt!
  distributionStrategy: String!
  addtlDataCid: String!
  contributorEligibility: String!
  verifierEligibility: String!
  allowedToken: String!
}
